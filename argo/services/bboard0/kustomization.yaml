apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Workload template for the `bboard0` service. Reusable across envs; the overlay
# adds the KEDA scaler + interceptor and drops the direct Ingress.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml

labels:
  - pairs:
      app.kubernetes.io/name: bboard0
    includeSelectors: false
